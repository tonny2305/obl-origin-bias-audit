# Style Pass Changelog

Total replacements: 81 (data, citations, equations, and numeric values unchanged in every case; table cells and the Table 1 legend symbol '—' = not applicable were intentionally left untouched, as those are data, not prose).

| # | Location | Change | Before -> After |
|---|---|---|---|
| 1 | Abstract | em dash -> comma (short appositive) | `of the benchmark suite — a distinction the literature rarely tests.` -> `of the benchmark suite, a distinction the literature rarely tests.` |
| 2 | Abstract | removed formulaic 'Crucially, however,'; em dash -> semicolon | `IFPOA-X attains the best mean Friedman rank (**1.31**). Crucially, however, a Holm-corrected post...` -> `IFPOA-X attains the best mean Friedman rank (**1.31**). A Holm-corrected post-hoc test, however, ...` |
| 3 | Abstract | em dash pair -> comma pair | `Since OBL reflects candidates through the domain centre — where the classical optima cluster — we...` -> `Because OBL reflects candidates through the domain centre, where the classical optima cluster, we...` |
| 4 | Abstract | split into two sentences for rhythm | `The advantage does not survive: IFPOA-X's rank collapses to **5.23–5.77 (5th–6th of 8)**` -> `The advantage does not survive. IFPOA-X's rank collapses to **5.23–5.77 (5th–6th of 8)**` |
| 5 | Abstract | em dash -> new sentence | `once the optimum is displaced (difference-in-differences significant, paired p = 4.9×10⁻⁴) — stro...` -> `once the optimum is displaced (difference-in-differences significant, paired p = 4.9×10⁻⁴). This ...` |
| 6 | Intro p1 | em dash pair -> comma pair | `Nature-inspired population-based metaheuristics — including Particle Swarm Optimization (PSO) [1]...` -> `Nature-inspired population-based metaheuristics, including Particle Swarm Optimization (PSO) [1],...` |
| 7 | Intro p2 | em dash pair -> colon + comma | `**large number of function evaluations (NFE)** — tens or hundreds of thousands of evaluations are...` -> `**large number of function evaluations (NFE)**: tens or hundreds of thousands of evaluations are ...` |
| 8 | Intro p3 | em dash pair removed, clause reordered | `Under such conditions only a small evaluation budget — typically on the order of hundreds, not te...` -> `Under such conditions only a small evaluation budget is practically affordable, typically on the ...` |
| 9 | Intro p3 | split long sentence; removed repeated 'Taken together' (1/2) | `Surrogate-assisted evolutionary algorithms extend the same principle to population-based search b...` -> `Surrogate-assisted evolutionary algorithms extend the same principle to population-based search b...` |
| 10 | Intro Why500 | em dash -> comma | `real function evaluations as its evaluation ceiling — roughly **one to two orders of magnitude sm...` -> `real function evaluations as its evaluation ceiling, roughly **one to two orders of magnitude sma...` |
| 11 | Intro Why500 | em dash pair -> comma pair; split sentence | `The intent is to *simulate* an expensive-evaluation setting: if a single evaluation is illustrati...` -> `The intent is to *simulate* an expensive-evaluation setting. If a single evaluation is illustrati...` |
| 12 | Intro Why500 | em dash -> comma | `how much solution quality an algorithm can extract per evaluation — which is the property this pa...` -> `how much solution quality an algorithm can extract per evaluation, which is the property this pap...` |
| 13 | Intro p_gap | em dash pair -> comma pair | `so broad, fair benchmarking under a controlled evaluation budget — rather than a handful of favou...` -> `so broad, fair benchmarking under a controlled evaluation budget, rather than a handful of favour...` |
| 14 | Intro p_geometry | em dash -> colon | `This concern is not speculative — Kudela [14] identifies inadequate benchmark design as a central...` -> `This concern is not speculative: Kudela [14] identifies inadequate benchmark design as a central,...` |
| 15 | Intro p_geometry | em dash -> comma | `because it operates by reflecting candidates through the domain centre — a design choice whose ef...` -> `because it operates by reflecting candidates through the domain centre, a design choice whose eff...` |
| 16 | Intro vehicle | restructured away from 'not X but Y'; two em dashes removed | `We are explicit that IFPOA-X is not offered as a recommended best-in-class optimizer — indeed its...` -> `We are explicit that IFPOA-X is not offered as a recommended best-in-class optimizer; its own abl...` |
| 17 | Intro contrib3 | em dash pair -> colon + new sentence | `We frame this as a general cautionary finding for the field — adaptive operator-control mechanism...` -> `We frame this as a general cautionary finding for the field: adaptive operator-control mechanisms...` |
| 18 | Intro contrib4 | em dash -> new sentence | `that the advantage is substantially suite-geometry-dependent rather than general-purpose — a vali...` -> `that the advantage is substantially suite-geometry-dependent rather than general-purpose. We beli...` |
| 19 | 2.1 | em dash -> new sentence | `while Mergos and Yang [5] add a pollinator-attraction bias that yields statistically significant ...` -> `while Mergos and Yang [5] add a pollinator-attraction bias that yields statistically significant ...` |
| 20 | 2.2 | em dash -> comma | `capable of misleading the search or proving inefficient specifically on symmetric landscapes — pr...` -> `capable of misleading the search or proving inefficient specifically on symmetric landscapes, pre...` |
| 21 | 2.2 | em dash -> comma + 'though' | `remaining one of the strongest DE variants on the CEC benchmarks — the caveat being that both wer...` -> `remaining one of the strongest DE variants on the CEC benchmarks, though both were designed and v...` |
| 22 | 2.3 | em dash -> colon | `Both enjoy enormous adoption, yet their standing is contested — a substantial critical literature...` -> `Both enjoy enormous adoption, yet their standing is contested: a substantial critical literature ...` |
| 23 | 2.4 | em dash -> new sentence | `and ASHA [11] makes the underlying successive-halving asynchronous and massively parallel — both ...` -> `and ASHA [11] makes the underlying successive-halving asynchronous and massively parallel. Both d...` |
| 24 | 2.5 | em dash -> colon | `which reframes the goal from "winning everywhere" to demonstrating a *specific*, well-characteriz...` -> `which reframes the goal from "winning everywhere" to demonstrating a *specific*, well-characteriz...` |
| 25 | 2.5 | em dash pair -> colon + new sentence | `Derrac et al. [23] provide the now-standard non-parametric methodology for comparing evolutionary...` -> `Derrac et al. [23] provide the now-standard non-parametric methodology for comparing evolutionary...` |
| 26 | 2.5 | removed repeated 'taken together' (2/2) -> 'Collectively' | `with claimed superiority frequently traceable to unequal budgets, weak baselines, or omitted stat...` -> `with claimed superiority frequently traceable to unequal budgets, weak baselines, or omitted stat...` |
| 27 | 2.5 | em dash -> comma | `can depend materially on the evaluation budget and performance measure chosen — the same protocol...` -> `can depend materially on the evaluation budget and performance measure chosen, the same protocol-...` |
| 28 | 2.6 | em dash -> comma; 'not in X' -> 'rather than in X' | `Synthesizing these threads reveals the gap this paper actually addresses — and it is a gap in *ev...` -> `Synthesizing these threads reveals the gap this paper actually addresses, a gap in *evaluation pr...` |
| 29 | 2.6 | em dash pair -> comma pair | `are characterized mainly at generous budgets, and — as the fair-comparison literature stresses [1...` -> `are characterized mainly at generous budgets, and, as the fair-comparison literature stresses [12...` |
| 30 | 2.6 | em dash -> comma | `OBL's centre-reflection operator is thus one concrete, mechanistically transparent instance of a ...` -> `OBL's centre-reflection operator is thus one concrete, mechanistically transparent instance of a ...` |
| 31 | 3 intro | em dash pair -> comma pair | `A fourth component of the broader framework — an optional surrogate/multi-fidelity screen — is **...` -> `A fourth component of the broader framework, an optional surrogate/multi-fidelity screen, is **di...` |
| 32 | 3.1 | em dash pair -> colon + new sentence | `combines deterministic annealing with the classical **one-fifth success rule** of evolution-strat...` -> `combines deterministic annealing with the classical **one-fifth success rule** of evolution-strat...` |
| 33 | 3.2 | em dash pair -> comma pair | `On a centrally symmetric function over symmetric bounds — as most of the classical F1–F13 suite i...` -> `On a centrally symmetric function over symmetric bounds, as most of the classical F1–F13 suite is...` |
| 34 | 3.2 | em dash -> colon | `is precisely what the origin-bias controls of §5.7 are designed to probe — displacing the optima ...` -> `is precisely what the origin-bias controls of §5.7 are designed to probe: displacing the optima o...` |
| 35 | 3.2 | em dash pair -> parentheses | `Framed in terms of algorithmic invariance, translation invariance — insensitivity of an algorithm...` -> `Framed in terms of algorithmic invariance, translation invariance (insensitivity of an algorithm'...` |
| 36 | 3.4 | em dash -> new sentence | `that evaluates candidates at increasing fidelity across successive rungs — both intended to amort...` -> `that evaluates candidates at increasing fidelity across successive rungs. Both are intended to am...` |
| 37 | 3.4 | em dash -> colon | `This is a deliberate scoping decision, reiterated in §5.6, not an oversight — this paper's contri...` -> `This is a deliberate scoping decision, reiterated in §5.6, not an oversight: this paper's contrib...` |
| 38 | 4.3 caching | em dash -> colon | `so caching neither saves IFPOA-X evaluations nor affects the count in practice — the hard cap cou...` -> `so caching neither saves IFPOA-X evaluations nor affects the count in practice: the hard cap coun...` |
| 39 | 4.3 init | em dash -> comma | `which concerns how IFPOA-X's *own* advantage changes across benchmark geometries (§5.7) — a withi...` -> `which concerns how IFPOA-X's *own* advantage changes across benchmark geometries (§5.7), a within...` |
| 40 | 4.4 budget | em dash pair -> comma pair | `it simulates the *expensive-evaluation* regime of real applications — such as hyperparameter opti...` -> `it simulates the *expensive-evaluation* regime of real applications such as hyperparameter optimi...` |
| 41 | 4.4 honesty | em dash pair -> comma pair | `its trade-off — that baselines might individually improve with problem-specific tuning — is discu...` -> `its trade-off, that baselines might individually improve with problem-specific tuning, is discuss...` |
| 42 | 5.1 per-fn | split long sentence into three; removed 2 em dashes | `IFPOA-X attains the best mean value on **12 of the 13 functions** — the sole exception is F8 (Sch...` -> `IFPOA-X attains the best mean value on **12 of the 13 functions**. The sole exception is F8 (Schw...` |
| 43 | 5.1 cross-fn | em dash pair -> colon + new sentence | `But the Holm-corrected post-hoc comparison against IFPOA-X (control) shows that its rank advantag...` -> `But the Holm-corrected post-hoc comparison against IFPOA-X (control) shows that its rank advantag...` |
| 44 | 5.1 cross-fn | em dash pair -> comma pair | `In other words, even on the classical suite — before any origin-bias control — IFPOA-X's superior...` -> `In other words, even on the classical suite, before any origin-bias control, IFPOA-X's superiorit...` |
| 45 | 5.1 hinge | restructured away from 'not X but Y' | `This is not a weakness to be hidden but the first hinge of the paper's argument: the two baseline...` -> `This is not a weakness to hide. It is the first hinge of the paper's argument: the two baselines ...` |
| 46 | 5.2 | em dash pair -> parentheses | `as §5.1 showed, the *cross-function* rank test — the appropriate unit for a suite-level conclusio...` -> `as §5.1 showed, the *cross-function* rank test (the appropriate unit for a suite-level conclusion...` |
| 47 | 5.4 setup | em dash pair -> colon + new sentence | `we ran four ablated variants — disabling OBL, disabling the JADE-style local search, disabling th...` -> `we ran four ablated variants: disabling OBL, disabling the JADE-style local search, disabling the...` |
| 48 | 5.4 setup | em dash -> comma | `so every ablated run is *paired* with its corresponding "full" run — enabling a paired Wilcoxon s...` -> `so every ablated run is *paired* with its corresponding "full" run, enabling a paired Wilcoxon si...` |
| 49 | 5.4 results | clarified awkward double-negative parenthetical; em dash removed; no data changed (still 1 win F8, 1 tie F7) | `**The JADE local search's isolated contribution is significantly *negative* on 11 of 13 functions...` -> `**The JADE local search's isolated contribution is significantly *negative* on 11 of 13 functions...` |
| 50 | 5.4 heading | em dash -> colon in inline heading | `**Why the adaptive components fail at this budget — a quantitative account.**` -> `**Why the adaptive components fail at this budget: a quantitative account.**` |
| 51 | 5.4 budget-math | em dash -> comma | `and the policy remains statistically indistinguishable from the uniform 50/50 selection it is com...` -> `and the policy remains statistically indistinguishable from the uniform 50/50 selection it is com...` |
| 52 | 5.6 untuned | em dash -> semicolon | `DE, L-SHADE, GWO, and WOA were run with their default `mealpy` hyperparameters, and TPE with defa...` -> `DE, L-SHADE, GWO, and WOA were run with their default `mealpy` hyperparameters, and TPE with defa...` |
| 53 | 5.6 budget-favour | em dash -> comma | `(F1, F4, F6 — simple unimodal landscapes, Supplementary Table S2) are the most plausible candidates` -> `(F1, F4, F6, simple unimodal landscapes, Supplementary Table S2) are the most plausible candidates` |
| 54 | 5.6 F8 | restructured away from 'not X but Y'; 2 em dashes removed | `**F8 foreshadows the origin-bias threat.** The single classical-suite loss — to WOA on F8, the on...` -> `**F8 foreshadows the origin-bias threat.** The single classical-suite loss, to WOA on F8, the one...` |
| 55 | 5.6 limitations-i | em dash -> semicolon | `and only for a 10-function subset — the three hand-constructed shifts (§5.7.1, §5.7.3) isolate or...` -> `and only for a 10-function subset; the three hand-constructed shifts (§5.7.1, §5.7.3) isolate ori...` |
| 56 | 5.6 limitations-iii | em dash -> semicolon | `dimensionality was tested only at D ∈ {30, 50} for the classical suite (§5.5) — none of the four ...` -> `dimensionality was tested only at D ∈ {30, 50} for the classical suite (§5.5); none of the four o...` |
| 57 | 5.6 limitations-vi | em dash pair -> comma pair | `requires a domain-safe construction for F8 (Schwefel 2.26) — the only suite function defined on a...` -> `requires a domain-safe construction for F8 (Schwefel 2.26), the only suite function defined on a ...` |
| 58 | 5.6 future-work | em dash pair -> comma pair | `and validating the complete IFPOA-X framework — surrogate screen and ASHA pruning enabled — on a ...` -> `and validating the complete IFPOA-X framework, with surrogate screen and ASHA pruning enabled, on...` |
| 59 | 5.7 intro | em dash -> new sentence | `We test this with **four complementary controls** — not statistically independent (three are buil...` -> `We test this with **four complementary controls**. They are not statistically independent (three ...` |
| 60 | 5.7.1 result | em dash -> comma | `on the shifted suite (Friedman χ² = 60.90, p = 1.0×10⁻¹⁰, CD = 2.91) — behind WOA, GWO, L-SHADE, ...` -> `on the shifted suite (Friedman χ² = 60.90, p = 1.0×10⁻¹⁰, CD = 2.91), behind WOA, GWO, L-SHADE, P...` |
| 61 | 5.7.1 CD | em dash -> comma | `while being **significantly worse** than WOA, GWO, and L-SHADE — precisely the algorithms with no...` -> `while being **significantly worse** than WOA, GWO, and L-SHADE, precisely the algorithms with no ...` |
| 62 | 5.7.1 CD | em dash -> comma | `and is roughly split against TPE (1/5/7 win/loss/tie) — but loses decisively to L-SHADE (0/11), G...` -> `and is roughly split against TPE (1/5/7 win/loss/tie), but loses decisively to L-SHADE (0/11), GW...` |
| 63 | 5.7.1 wrap | em dash -> new sentence | `Control 1 shows that IFPOA-X did not simply get *weaker* under a harder benchmark — it was overta...` -> `Control 1 shows that IFPOA-X did not simply get *weaker* under a harder benchmark. It was overtak...` |
| 64 | 5.7.2 subset | em dash pair -> comma pair | `(numbered per the specification), chosen — before seeing any results — to span all four official ...` -> `(numbered per the specification), chosen before seeing any results, to span all four official cat...` |
| 65 | 5.7.2 notably | removed formulaic 'Notably,'; em dash -> new sentence | `even after multiplicity correction. Notably, the *specific* baseline that overtakes IFPOA-X diffe...` -> `even after multiplicity correction. The *specific* baseline that overtakes IFPOA-X differs from C...` |
| 66 | 5.7.2 wrap | em dash -> semicolon | `while the "IFPOA-X loses its rank-1 status once the origin-bias is removed" finding is not — that...` -> `while the "IFPOA-X loses its rank-1 status once the origin-bias is removed" finding is not; that ...` |
| 67 | 5.7.3 f8 | em dash pair -> comma pair | `a signed shift can push the effective argument $x - o$ outside that box, where the guarantee — an...` -> `a signed shift can push the effective argument $x - o$ outside that box, where the guarantee, and...` |
| 68 | 5.7.3 result | em dash -> semicolon | `mean Friedman rank 5.23 of 8 ("far", winner WOA at 1.23) and 5.31 of 8 ("mixed", winner GWO at 1....` -> `mean Friedman rank 5.23 of 8 ("far", winner WOA at 1.23) and 5.31 of 8 ("mixed", winner GWO at 1....` |
| 69 | 5.7.4 | em dash pair -> comma pair | `Four independently constructed controls — one community-standard (CEC2017) and three internally c...` -> `Four independently constructed controls, one community-standard (CEC2017) and three internally co...` |
| 70 | 5.7.5 open | em dash pair -> colon + new sentence | `The controls above establish two facts *separately* — OBL drives IFPOA-X's advantage on centred s...` -> `The controls above establish two facts *separately*: OBL drives IFPOA-X's advantage on centred su...` |
| 71 | 5.7.5 causal | em dash -> comma | `it provides **strong causal evidence for the role of OBL within the tested IFPOA-X implementation...` -> `it provides **strong causal evidence for the role of OBL within the tested IFPOA-X implementation...` |
| 72 | 5.7.5 delta | em dash pair -> comma pair | `The interaction — the difference between the two marginal benefits — is ≈ 3.97 orders of magnitud...` -> `The interaction, the difference between the two marginal benefits, is ≈ 3.97 orders of magnitude ...` |
| 73 | 5.7.5 tellingly | em dash pair -> comma pair | `Tellingly, the single centred function on which OBL provides *no* benefit is F8 (Δ ≈ 0) — the one...` -> `Tellingly, the single centred function on which OBL provides *no* benefit is F8 (Δ ≈ 0), the one ...` |
| 74 | 5.7.5 strongest | restructured away from 'not X but Y'; em dash -> new sentence | `**the elite-opposition operator's benefit is not a general search improvement but a conditional r...` -> `**the elite-opposition operator's benefit is a conditional response to origin-centred geometry ra...` |
| 75 | 5.7.5 principal | em dash -> comma | `as the paper's principal methodological contribution — more important than the classical-suite ra...` -> `as the paper's principal methodological contribution, more important than the classical-suite ran...` |
| 76 | 5.7.5 caution | em dash pair -> colon + new sentence | `what the factorial establishes universally is a *cautionary existence result* — a widely used mec...` -> `what the factorial establishes universally is a *cautionary existence result*: a widely used mech...` |
| 77 | 6 p1 | em dash -> new sentence | `and wins 12 of 13 functions per-baseline in per-function Wilcoxon tests — yet under Holm correcti...` -> `and wins 12 of 13 functions per-baseline in per-function Wilcoxon tests. Yet under Holm correctio...` |
| 78 | 6 p2 open | em dash -> comma | `its benefit on a centrally symmetric landscape depends on the optimum sitting near that centre (§...` -> `its benefit on a centrally symmetric landscape depends on the optimum sitting near that centre (§...` |
| 79 | 6 p2 limits | em dash pair -> parentheses | `The limitations that bound this conclusion — shift-only controls for three of the four suites, no...` -> `The limitations that bound this conclusion (shift-only controls for three of the four suites, no ...` |
| 80 | 6 p3 urge | em dash -> comma | `be treated as unverified until checked against a shifted control — and that reviewers and venues ...` -> `be treated as unverified until checked against a shifted control, and that reviewers and venues r...` |
| 81 | 6 p3 pcobl | em dash -> comma | `would remove the specific coupling to benchmark geometry this paper identifies — at the cost of t...` -> `would remove the specific coupling to benchmark geometry this paper identifies, at the cost of th...` |

## Round 2 (remaining genuine prose em dashes)

| # | Location | Change | Before -> After |
|---|---|---|---|
| 82 | Intro contrib-list | em dash pair -> comma pair | `IFPOA-X is evaluated against seven baselines — FPA [3], PSO [1], DE [2], L-SHADE [19], GWO [20], ...` -> `IFPOA-X is evaluated against seven baselines, FPA [3], PSO [1], DE [2], L-SHADE [19], GWO [20], W...` |
| 83 | 2 provenance note | em dash -> semicolon | `and each record was screened for retractions, corrections, or editorial concerns — none of the se...` -> `and each record was screened for retractions, corrections, or editorial concerns; none of the sel...` |
| 84 | 4.4 params | em dash -> semicolon | `(TPE, being sequential model-based, has no population — see §4.3); and **20 independent runs**` -> `(TPE, being sequential model-based, has no population; see §4.3); and **20 independent runs**` |
| 85 | 5.1 boxplot | em dash -> comma | `with its only excursion to rank 2 corresponding to the F8 exception discussed above — visual conf...` -> `with its only excursion to rank 2 corresponding to the F8 exception discussed above, visual confi...` |
| 86 | 5.3 convergence | em dash -> comma | `whereas the baselines plateau early — a signature of premature convergence.` -> `whereas the baselines plateau early, a signature of premature convergence.` |
| 87 | 5.6 limitations-iv | em dash pair -> comma pair | `(iv) the surrogate/ASHA module — the part of the framework that would justify an *expensive-evalu...` -> `(iv) the surrogate/ASHA module, the part of the framework that would justify an *expensive-evalua...` |
| 88 | 5.7.1 construction a | em dash pair -> comma pair | `we verify $g(o)=0$ numerically for every function — including the non-origin cases Rosenbrock (F5...` -> `we verify $g(o)=0$ numerically for every function, including the non-origin cases Rosenbrock (F5)...` |
| 89 | 5.7.1 construction b | em dash -> semicolon | `Domain bounds, dimension, budget, equal-NFE protocol, and all algorithm hyperparameters (Suppleme...` -> `Domain bounds, dimension, budget, equal-NFE protocol, and all algorithm hyperparameters (Suppleme...` |
| 90 | 5.7.1 result | em dash pair -> comma pair | `WOA — an algorithm with no centre-reflection mechanism — wins all **13 of 13** shifted functions ...` -> `WOA, an algorithm with no centre-reflection mechanism, wins all **13 of 13** shifted functions ou...` |
| 91 | 6 p1 ablation | em dash pair -> comma pair; minor smoothing ('shows the advantage that does exist is' -> 'shows that the advantage which does exist is') | `A full-power ablation — 20 paired runs across all 13 functions — further shows the advantage that...` -> `A full-power ablation, 20 paired runs across all 13 functions, further shows that the advantage w...` |

## Round 3 (reference-list editorial annotations, not bibliographic metadata)

| # | Location | Change | Before -> After |
|---|---|---|---|
| 92 | Ref [38] annotation | em dash -> colon | `*(Software reference — library used to run the DE...` -> `*(Software reference: library used to run the DE...` |
| 93 | Ref [39] annotation | em dash -> colon | `*(Software reference — library used to run the TPE...` -> `*(Software reference: library used to run the TPE...` |
| 94 | Ref [40] annotation | em dash -> colon | `*(Software reference — library providing the CEC2017...` -> `*(Software reference: library providing the CEC2017...` |

## Round 4 (repeated 'not X but Y' construction, echoing an instance already varied in §5.7.5)

| # | Location | Change | Before -> After |
|---|---|---|---|
| 95 | Conclusion p1 | reordered clause to avoid repeating the same rhetorical construction used (and already varied) in §5.7.5 | `...is not a diffuse property of "combined mechanisms" but is attributable...` -> `...is attributable almost entirely to Opposition-Based Learning, not to a diffuse property of "combined mechanisms"` |
